# -----------------------------
# Raw Material Marketplace API
# Vercel Deployment – example environment file
# Rename to ".env" in local development or add each key in Vercel → Project → Settings → Environment Variables.
# -----------------------------

# GENERAL
NODE_ENV=production
PORT=5000
FRONTEND_URL=https://raw-material-market-frontend.vercel.app
APP_URL=${FRONTEND_URL}

# DATABASE
# Provide a connection string for a production MongoDB deployment (e.g. MongoDB Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/raw-material-marketplace?retryWrites=true&w=majority

# AUTH / JWT
JWT_SECRET=replace_this_with_long_random_string
JWT_EXPIRE=7d

# CLOUDINARY (image hosting)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# STRIPE (payments – optional)
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

# EMAIL SMTP (optional – for verification / notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=you@example.com
EMAIL_PASS=your_email_password_or_app_password

# -----------------------------
# Add any additional keys you integrate later
# -----------------------------
